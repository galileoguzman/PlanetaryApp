<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:PlanetaryApp" x:Class="PlanetaryApp.MainPage">
    <ListView x:Name="planetsListView" RowHeight="80" HasUnevenRows="true">
        <!--NEW LIST VIEW ITEM TEMPLATE LIKE A FOREACH -->
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Name}" TextColor="Black" Detail="{Binding Distance}" DetailColor="Gray"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>